
export default {
  plugins: {
    // 1. You must use the new package path: '@tailwindcss/postcss'
    '@tailwindcss/postcss': {}, 
    
    // 2. Keep standard plugins like autoprefixer
    'autoprefixer': {},
  },
}
